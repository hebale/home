export function setPoints({ x, y, z }){
  return [
    x[1], y[1], z[1],
    x[0], y[0], z[1],
    x[0], y[1], z[1],
    
    x[1], y[1], z[1],
    x[1], y[0], z[1],
    x[0], y[0], z[1],
    
    x[0], y[1], z[1],
    x[0], y[0], z[0],
    x[0], y[1], z[0],
    
    x[0], y[1], z[1],
    x[0], y[0], z[1],
    x[0], y[0], z[0],
    
    x[1], y[1], z[0],
    x[1], y[0], z[1],
    x[1], y[1], z[1],
    
    x[1], y[1], z[0],
    x[1], y[0], z[0],
    x[1], y[0], z[1],
    
    x[0], y[1], z[0],
    x[1], y[0], z[0],
    x[1], y[1], z[0],
    
    x[0], y[1], z[0],
    x[0], y[0], z[0],
    x[1], y[0], z[0],
  ];
};

export function setPointCap({ x, y, z }){
  return [
    x[1], y[0], z[1],
    x[0], y[0], z[0],
    x[0], y[0], z[1],

    x[1], y[0], z[1],
    x[1], y[0], z[0],
    x[0], y[0], z[0],

    x[1], y[1], z[0],
    x[0], y[1], z[1],
    x[0], y[1], z[0],

    x[1], y[1], z[0],
    x[1], y[1], z[1],
    x[0], y[1], z[1]
  ]
};









export const point = new Float32Array([
  1, 1, 1, // 0
  -1, -1, 1, // 1
  -1, 1, 1,  // 2
  1, -1, 1,  // 3
  -1, -1, -1, // 4
  -1, 1, -1, // 5
  1, 1, -1, // 6
  1, -1, -1, // 7
  -1, 1, -1, // 8
]);

export const indices = new Uint16Array([
  0, 1, 2, 0, 3, 1, // 앞
  2, 4, 5, 2, 1, 4, // 좌
  6, 3, 0, 6, 7, 3, // 우
  8, 7, 6, 8, 4, 7, // 뒤
  3, 4, 1, 3, 7, 4, // 밑
  6, 2, 8, 6, 0, 2  // 위
]);

export const uvs = new Float32Array([
  0.0, 0.0,
  1.0, 0.0,
  1.0, 1.0,
  0.0, 1.0,
]);